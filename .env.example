# Contact email embedded in the crawler User-Agent (politeness/accountability).
# Set as a GitHub repository variable CONTACT_EMAIL for CI, or export locally.
CONTACT_EMAIL=
# Production site origin, e.g. https://yourdomain.example — set as repo variable SITE_URL.
SITE_URL=
