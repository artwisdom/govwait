---
// Reserved ad slot. Intentionally renders NOTHING until the owner activates a
// network (no layout shift, no placeholder pixels, no publisher IDs in repo).
const { position } = Astro.props;
---
<!-- AD_SLOT position={position}: activate after ad network approval — see DEPLOYMENT_GUIDE -->
